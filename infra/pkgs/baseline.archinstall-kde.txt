# archinstall KDE Plasma profile baseline (NOT installed by archway)
# ─────────────────────────────────────────────────────────────────────
# This file is **not** consumed by bootstrap.sh. Its sole purpose is to
# tell `infra/doctor.sh --audit-packages` which packages are expected
# to be present on the system because they were installed by the
# `archinstall` KDE Plasma desktop profile (see docs/SETUP.md §1.7).
#
# Without this allowlist, every package pulled in by `plasma-meta`
# would show up as "untracked" in the audit report, drowning out
# real drift signal.
#
# Source of truth: archinstall ≥4.2 source
#   https://github.com/archlinux/archinstall  →  archinstall/default_profiles/desktops/kde.py
# Re-derive after a Plasma major version bump:
#   pacman -Sg plasma | awk '{print $2}' | sort -u
#
# Format: one package per line, comments with #, alphabetized.

# ── Metapackage that pulls the rest of the `plasma` group ────────────
plasma-meta

# ── Members of the `plasma` group (pacman -Sg plasma) ────────────────
# Listed explicitly so the audit doesn't depend on `pacman -Sg` at
# audit time (keeps doctor.sh hermetic and offline-safe).
bluedevil
breeze
breeze-gtk
discover
drkonqi
flatpak-kcm
kactivitymanagerd
kde-cli-tools
kde-gtk-config
kdecoration
kdeplasma-addons
kgamma
kglobalacceld
kinfocenter
kmenuedit
kpipewire
krdp
kscreen
kscreenlocker
ksshaskpass
ksystemstats
kwallet-pam
kwayland
kwin
kwrited
layer-shell-qt
libkscreen
libksysguard
libplasma
milou
ocean-sound-theme
oxygen
oxygen-sounds
plasma-activities
plasma-activities-stats
plasma-browser-integration
plasma-desktop
plasma-disks
plasma-firewall
plasma-integration
plasma-nm
plasma-pa
plasma-sdk
plasma-systemmonitor
plasma-thunderbolt
plasma-vault
plasma-welcome
plasma-workspace
plasma-workspace-wallpapers
polkit-kde-agent
powerdevil
print-manager
qqc2-breeze-style
sddm-kcm
spectacle
systemsettings
xdg-desktop-portal-kde

# ── KDE applications added by the archinstall KDE profile on top ─────
# (kde-applications-meta subset; archinstall picks a small curated set)
ark
dolphin
ffmpegthumbs
gwenview
kate
kcalc
kcharselect
kcolorchooser
kdeconnect
kdegraphics-thumbnailers
kfind
khelpcenter
konsole
kwalletmanager
okular
partitionmanager

# ── Display manager + audio (also pulled by the profile) ─────────────
sddm
