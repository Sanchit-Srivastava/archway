# LaTeX / TeX toolchain + PDF viewer backend
#
# This is intentionally separated from the main tiers.
# TeX Live is very large and slow to install. It is not a priority
# during a "desperate reinstall" on limited bandwidth.
#
# Install later with: ./infra/install-tex.sh  or  just tex
#
# Keep entries alphabetized within sections. Comments with #.

# LaTeX / TeX toolchain + PDF viewer (heavy)
#
# This file is NOT included in the main tiered bootstrap by design.
# Run `./infra/install-tex.sh` or `just tex` after you have a basic system.
#
# See the comment in infra/pkgs/30-desktop.txt.

zathura
zathura-pdf-mupdf
biber

# TeX Live groups
texlive-basic
texlive-binextra
texlive-bibtexextra
texlive-fontsrecommended
texlive-latex
texlive-latexextra
texlive-pictures
texlive-plaingeneric
texlive-publishers
texlive-fontsextra

# Neovim integration for vimtex + SyncTeX
python-pynvim
