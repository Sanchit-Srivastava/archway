# Tier 3 (desktop) systemd units to enable system-wide
# KDE Plasma fallback graphical session

# =============================================================================
# DISPLAY MANAGER
# =============================================================================
# Intentionally NOT listed. archinstall's KDE profile already enables a display
# manager (SDDM, or Plasma Login Manager on newer releases). archway accepts
# whichever one archinstall enabled rather than enabling/swapping it here.
# To change your DM, do it deliberately (enable the new one, disable the old),
# not via bootstrap.

# =============================================================================
# PRINTING (Optional - uncomment if you use printers)
# =============================================================================
# cups
# cups-browsed
# avahi-daemon
