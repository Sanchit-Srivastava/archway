# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db

# Editor/IDE
.idea/
.vscode/
*.swp
*.swo
*~
*.bak
*.orig

# Temporary files
*.tmp
*.temp

# JVM crash dumps (e.g. from the ltex-ls / LanguageTool language server crashing
# inside an editor). These land in the CWD wherever the editor was launched and
# are unrelated to the repo; never commit them.
hs_err_pid*.log
replay_pid*.log

# Install scripts (one-time wrappers)
# install-dms.sh  # Keep in repo for target laptop

# Environment
.env
.env.local
*.pem
*.key
dots/opencode/.env

# Reference repos (for development only)
reference/

# Neovim plugin lockfile: deliberately untracked so each fresh install
# pulls the latest plugin commits via `:Lazy sync` on first launch.
# Committing the lockfile would freeze plugins to whatever the dev machine
# happened to have, which is rarely what we want.
dots/nvim/lazy-lock.json
